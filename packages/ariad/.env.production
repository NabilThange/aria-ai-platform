# ariad Production Environment Variables (Railway)
# Copy these to Railway Dashboard → Variables Tab

# Backend API Connection (optional - if ariad needs to call back to aria-agent)
ARIA_AGENT_BASE_URL=https://aria-agent.onrender.com

# Port (Railway sets this automatically, but you can specify if needed)
# PORT=9990
